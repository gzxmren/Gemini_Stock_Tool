# 项目开发经验与架构准则 (Project Lessons & Architectural Principles)

本文件记录了在开发过程中积累的关键教训，所有后续开发任务必须严格遵守这些准则，以避免重复犯错。

## 1. 架构准则：性能优于便利
- **拒绝过度抓取 (No Over-fetching)**：严禁在一个基础行情接口中塞入财报、DCF等深度分析数据。
- **坚持按需加载 (Lazy Loading)**：首屏接口必须保持极简（<500ms响应）。复杂数据应由前端组件在挂载时发起独立异步请求。
- **职责解耦**：行情、K线、财报、情报四个模块的数据流必须完全独立，互不阻塞。

## 2. 健壮性准则：零信任外部 API (Zero-Trust Data)
- **强制类型清洗**：永远不要信任第三方 API（如 yfinance）的数据类型。在渲染路径上必须执行强制转换：
  - 数值：`Number(val) || 0`
  - 字符串：`String(val || '')`
- **样式安全检查**：涉及 CSS 布局的计算（如仪表盘分位、K线偏移）必须包裹 `Math.min/max` 并检查 `isFinite`，严禁出现 `NaN%` 导致 React 树崩溃。

## 3. 并发准 duo ：隔离阻塞，保护数据库
- **线程池白名单**：仅允许纯粹的阻塞式网络 IO（如同步爬虫库调用）进入 `run_in_executor` 线程池。
- **数据库主线程安全**：SQLAlchemy Session 是非线程安全的。**严禁**跨线程共享 Session。所有数据库读写必须在 FastAPI 路由的主协程/主线程中顺序执行。

## 4. 业务准则：金融逻辑的严谨性
- **币种意识 (Currency Awareness)**：处理非本土股票（如 ADR）时，必须检测 `Report Currency` 与 `Trade Currency` 是否一致。若不一致，必须强制执行自动汇率转换（如 TWD -> USD）。
- **量级校验**：对于计算得出的估值数据，需进行数量级常识检查，防止因单位错误（如万亿台币误当作美元）产生荒谬结果。

## 5. 流程准则：重构必须验证全链路
- **全链路沙盘测试**：在大规模修改后端逻辑前，必须编写独立的流水线脚本（如 `test_pipeline.py`）模拟“抓取 -> 提取 -> 映射”的全过程。
- **回归测试**：每次修改路由或接口定义后，必须强制运行 `pytest` 并执行前端生产环境编译 (`npm run build`)，以捕捉前后端协议不匹配导致的错误。
